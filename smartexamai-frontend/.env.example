# URL de base de l'API FastAPI.
# En développement avec le proxy Vite ci-dessus, vous pouvez laisser vide
# (les requêtes /api/* passeront par le proxy). En production, pointez
# vers l'URL réelle du backend déployé.
VITE_API_URL=http://localhost:8000
